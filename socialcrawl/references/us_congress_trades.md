# US Congress Trades

19 endpoints, all GET. Base URL `https://www.socialcrawl.dev`, auth header `x-api-key: $SOCIALCRAWL_API_KEY`.

**Credit costs:** 19 standard (1 credit) - the exact cost is in each endpoint heading below.

US Congress STOCK Act disclosures - what members of Congress bought and sold, when they filed it, and how late. Trade feeds (everything, last 48 hours, last 7 days), the members who have disclosed, per-politician and per-ticker views, state-delegation trades, and a statistics suite covering party, sector, issuer, most-active politicians, most-traded tickers, volume over time, unusual activity, buy/sell ratio, and late-filing reporting gaps. Every endpoint is 1 credit.

<!-- BEGIN:GENERATED:ENDPOINTS (auto-generated by generate-docs.ts - AIP-16b) -->

## GET /v1/us_congress_trades/members - 1 credit (standard)

List members of Congress who have disclosed trades

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns a directory of politicians with at least one STOCK Act disclosure: first name, last name, party, and state. Filter by party or USPS state. Pass last name as handle to GET /v1/us_congress_trades/politician.

**Query params**

- `party` (optional, enum: democrat | republican) - democrat or republican. · e.g. `democrat`
- `state` (optional, string) - Two-letter USPS state, for example CA or TX. · e.g. `ca`
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 50.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/members" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/politician - 1 credit (standard)

Get a politician's trading summary

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Returns aggregated STOCK Act stats for one politician by last name: party, state, trade count, buy vs sell, disclosed value, average reporting gap, top tickers, and top sectors. Pass the same handle to GET /v1/us_congress_trades/politician/trades for the filings themselves.

**Query params**

- `handle` (required) - Politician last name, case-insensitive. Example: Pelosi. · e.g. `Pelosi`

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/politician?handle=Pelosi" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/politician/trades - 1 credit (standard)

List trades for one politician

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns every STOCK Act disclosure for one politician, keyed by last name. Same row shape as GET /v1/us_congress_trades/trades. Sort by publication date, trade date, value, or reporting gap.

**Query params**

- `handle` (required) - Politician last name, case-insensitive. Example: Pelosi. · e.g. `Pelosi`
- `sort` (optional, enum: pub_date | tx_date | value | reporting_gap) - pub_date (default), tx_date, value, or reporting_gap.
- `order` (optional, enum: asc | desc) - desc (default) or asc.
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 20.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/politician/trades?handle=Pelosi" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/state/trades - 1 credit (standard)

List trades from a state's congressional delegation

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns STOCK Act disclosures filed by members from one US state. Pass the two-letter USPS code (CA, TX, NY).

**Query params**

- `state` (required) - Two-letter USPS state, for example CA or TX. · e.g. `ca`
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 20.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/state/trades?state=ca" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats - 1 credit (standard)

Dataset summary

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Returns high-level STOCK Act coverage: total trades, total politicians, earliest and latest trade dates, and last updated time.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/buy-sell - 1 credit (standard)

Congressional buy vs sell ratio

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Returns buy vs sell counts, disclosed value, and the ratio, with optional party and sector filters plus a by-party and by-sector breakdown.

**Query params**

- `days` (optional, integer, 1-3650) - Lookback in days, 1 to 3650. Default 365. · e.g. `90`
- `party` (optional, enum: democrat | republican) - democrat or republican. · e.g. `democrat`
- `sector` (optional, string) - Issuer sector slug, for example information-technology.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/buy-sell" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/issuers - 1 credit (standard)

Most-traded issuers in Congress

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Top-N issuer dump (limit 1-50). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns companies ranked by disclosed congressional trade count, including Treasuries and private issuers that have no ticker.

**Query params**

- `limit` (optional, integer, 1-50) - How many issuers to return, 1 to 50. Default 10. · e.g. `10`

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/issuers" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/party - 1 credit (standard)

Compare trade counts by party

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Party totals are one dump (3 rows live). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns total disclosed trades grouped by party (democrat, republican, other).

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/party" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/politicians - 1 credit (standard)

Most-active congressional traders

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Top-N politician dump (limit 1-50). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns politicians ranked by disclosed trade count, with party.

**Query params**

- `limit` (optional, integer, 1-50) - How many politicians to return, 1 to 50. Default 10. · e.g. `10`

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/politicians" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/reporting-gaps - 1 credit (standard)

Politicians ranked by late STOCK Act filings

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns politicians ranked by average days between the trade date and the public filing, with max gap and trade count. Filter by chamber.

**Query params**

- `chamber` (optional, enum: senate | house) - senate or house. · e.g. `senate`
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 20.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/reporting-gaps" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/sectors - 1 credit (standard)

Trade counts by industry sector

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Sector totals are one dump (11 rows live). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns disclosed-trade counts grouped by issuer sector (information-technology, financials, health-care, and the rest of the GICS-style set).

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/sectors" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/tickers - 1 credit (standard)

Most-traded tickers in Congress

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Top-N ticker dump (limit 1-50). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns stock tickers ranked by disclosed congressional trade count, with buy vs sell split. Optionally restrict to buys or sells.

**Query params**

- `tx_type` (optional, enum: buy | sell) - buy or sell. Omit for both. · e.g. `buy`
- `limit` (optional, integer, 1-50) - How many tickers to return, 1 to 50. Default 10.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/tickers" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/unusual - 1 credit (standard)

Tickers with unusual congressional activity

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Unusual-activity dump (limit 1-50). There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns tickers whose recent disclosed-trade count is well above their historical average, with the politicians involved.

**Query params**

- `days` (optional, integer, 1-365) - Recent window in days, 1 to 365. Default 30. · e.g. `30`
- `limit` (optional, integer, 1-50) - How many tickers to return, 1 to 50. Default 10.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/unusual" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/stats/volume - 1 credit (standard)

Congressional trade volume over time

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Volume series is one dump for the requested days/period. There is no page or cursor param.
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns disclosed trade counts, buy vs sell, and total value grouped by day, week, or month over a lookback window.

**Query params**

- `days` (optional, integer, 7-3650) - Lookback in days, 7 to 3650. Default 365. · e.g. `90`
- `period` (optional, enum: day | week | month) - day, week (default), or month. · e.g. `week`

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/stats/volume" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/ticker - 1 credit (standard)

Get congressional trading stats for a ticker

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Returns aggregated STOCK Act stats for one stock: trade count, buy vs sell, disclosed value, unique politicians, and the members who trade it most. Accepts a bare ticker (AAPL) or AAPL:US. Pass the same keyword to GET /v1/us_congress_trades/ticker/trades for the filings.

**Query params**

- `keyword` (required) - Stock ticker. AAPL or AAPL:US. · e.g. `AAPL`

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/ticker?keyword=AAPL" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/ticker/trades - 1 credit (standard)

List congressional trades for a ticker

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns STOCK Act disclosures for one stock. Filter by chamber, party, transaction type, and date range. Accepts a bare ticker (AAPL) or AAPL:US.

**Query params**

- `keyword` (required) - Stock ticker. AAPL or AAPL:US. · e.g. `NVDA`
- `chamber` (optional, enum: senate | house) - senate or house.
- `party` (optional, enum: democrat | republican) - democrat or republican.
- `tx_type` (optional, enum: buy | sell | exchange) - buy, sell, or exchange. · e.g. `buy`
- `start_date` (optional, string) - Date lower bound (YYYY-MM-DD).
- `end_date` (optional, string) - Date upper bound (YYYY-MM-DD).
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 20.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/ticker/trades?keyword=NVDA" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/trades - 1 credit (standard)

Search US Congress stock trades

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns STOCK Act disclosures across the House and Senate. Filter by ticker (AAPL or AAPL:US), politician last name, party, chamber, sector, transaction type, and publication date range. Sort by publication date, trade date, disclosed value, or reporting gap. Page with offset. Each row has the politician, issuer, ticker, buy/sell/exchange, disclosed value, dates, and reporting gap in days.

**Query params**

- `ticker` (optional, string) - Stock ticker. Bare symbols such as AAPL are sent as AAPL:US. Use AAPL:US when the search filter would otherwise miss. · e.g. `NVDA`
- `politician` (optional, string) - Politician last name (case-insensitive).
- `party` (optional, enum: democrat | republican) - democrat or republican.
- `chamber` (optional, enum: senate | house) - senate or house. · e.g. `senate`
- `sector` (optional, string) - Issuer sector slug, for example information-technology or health-care.
- `tx_type` (optional, enum: buy | sell | exchange) - buy, sell, or exchange. · e.g. `buy`
- `start_date` (optional, string) - Publication date lower bound (YYYY-MM-DD).
- `end_date` (optional, string) - Publication date upper bound (YYYY-MM-DD).
- `sort` (optional, enum: pub_date | tx_date | value | reporting_gap) - pub_date (default), tx_date, value, or reporting_gap.
- `order` (optional, enum: asc | desc) - desc (default) or asc.
- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 20.
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/trades" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/trades/latest - 1 credit (standard)

Latest US Congress trades (48 hours)

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns STOCK Act disclosures published in the last 48 hours, newest first. Same row shape as GET /v1/us_congress_trades/trades. For a 7-day window use trades/recent.

**Query params**

- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 50. · e.g. `20`
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/trades/latest" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/us_congress_trades/trades/recent - 1 credit (standard)

Recent US Congress trades (7 days)

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** offset - `offset`, page size `limit`
**Empty results** An upstream 404 means "zero items", not "not found" - you get `200 {items: []}` and a refund.

Returns STOCK Act disclosures published in the last 7 days, newest first. Same row shape as GET /v1/us_congress_trades/trades. For the last 48 hours use trades/latest.

**Query params**

- `limit` (optional, integer, 1-100) - Page size, 1 to 100. Default 50. · e.g. `20`
- `offset` (optional, integer, min 0) - Rows to skip. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/us_congress_trades/trades/recent" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

<!-- END:GENERATED:ENDPOINTS -->
