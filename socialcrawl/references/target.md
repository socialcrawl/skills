# Target

5 endpoints, all GET. Base URL `https://www.socialcrawl.dev`, auth header `x-api-key: $SOCIALCRAWL_API_KEY`.

**Credit costs:** 2 standard (1 credit), 3 advanced (5) - the exact cost is in each endpoint heading below.

Target catalog data. Product detail, category browse, reviews, and physical store lookup. Shares the canonical Product / Review / Place schemas with the other retail platforms.

<!-- BEGIN:GENERATED:ENDPOINTS (auto-generated by generate-docs.ts - AIP-16b) -->

## GET /v1/target/categories - 1 credit (standard)

List the Target category taxonomy

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - The upstream returns the COMPLETE top-level taxonomy in one call (29 of 29 nodes, verified live 2026-07-27) and exposes no page parameter.

Returns Target's top-level browse taxonomy: each node's id, display name, parent id, canonical URL, and hero image. Feed a node id into GET /v1/target/category to walk that category's products. This is the entry point for TCIN discovery, since Target ships no keyword search we are willing to bill for. Static reference data, so it is 1 credit and heavily cached.

```bash
curl "https://www.socialcrawl.dev/v1/target/categories" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/target/category - 5 credits (advanced)

Browse Target products in a category

**Cost** 5 credits (advanced) · **Cache** 120s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** page - `page`

Returns products listed in a Target category, 24 per page, each with title, brand, price, regular price, rating, image gallery, highlight bullets, and its TCIN. Unlike Target's keyword search this one paginates, so it is the way to walk a whole category: pass the returned cursor back until has_more is false. Because Target ships no keyword search worth billing for, this plus GET /v1/target/categories is how you discover TCINs. Two things to expect: consecutive pages occasionally repeat a handful of products, because Target's category ordering shifts between calls, so de-duplicate by product.id when crawling; and ratings are present on roughly half of category rows and null on the rest, which reflects Target's own data rather than an omission here.

**Query params**

- `category_id` (required) - Target category node id, for example hz89j for Kitchen & Dining. Ids come from GET /v1/target/categories, and also appear in the category URLs on target.com (/c/<name>/-/N-<id>). · e.g. `hz89j`
- `page` (optional, integer, 1-100) - Page number, starting at 1. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/target/category?category_id=hz89j" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/target/product - 5 credits (advanced)

Get a Target product by TCIN

**Cost** 5 credits (advanced) · **Cache** 600s (a hit costs 0 credits) · **Returns** Product · **Pagination** none

Returns full product detail for a Target item: title, brand, description, highlight bullets, current and regular price, average rating with its 1-5 star distribution, written-review count, the full image gallery, colour and size variations, and stock status. Prices are national: Target's product endpoint reports the same price whatever store is asked for, so treat this as catalogue pricing rather than shelf pricing. For this product's written reviews call GET /v1/target/reviews with the same TCIN.

**Query params**

- `tcin` (required) - Target catalogue id (TCIN), the numeric id at the end of a target.com product URL: target.com/p/<name>/-/A-92148487 is TCIN 92148487. Target rolls a colour or size variant up to its parent product, so the id returned by GET /v1/target/product can differ from the one you sent; the id you asked for is preserved at product.ext.requested_id. · e.g. `92148487`

```bash
curl "https://www.socialcrawl.dev/v1/target/product?tcin=92148487" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/target/reviews - 5 credits (advanced)

Get Target product reviews

**Cost** 5 credits (advanced) · **Cache** 300s (a hit costs 0 credits) · **Returns** ReviewList · **Pagination** page - `page`

Returns written customer reviews for a Target product, 10 per page, each with the full review text, star rating, reviewer nickname, submission date, helpful-vote count, verified-purchase flag, and any reviewer photos. Pages do not overlap and the review total is exact, so a full crawl is deterministic: pass the returned cursor back until has_more is false. Page size is fixed by the upstream at 10 and cannot be raised. If you want the star distribution rather than the review text, call GET /v1/target/product instead and read product.ext.rating_distribution, which is one call rather than one per ten reviews.

**Query params**

- `tcin` (required) - Target catalogue id (TCIN), the numeric id at the end of a target.com product URL: target.com/p/<name>/-/A-92148487 is TCIN 92148487. Target rolls a colour or size variant up to its parent product, so the id returned by GET /v1/target/product can differ from the one you sent; the id you asked for is preserved at product.ext.requested_id. · e.g. `92146817`
- `page` (optional, integer, 1-500) - Page number, starting at 1. Prefer the universal cursor parameter.

```bash
curl "https://www.socialcrawl.dev/v1/target/reviews?tcin=92146817" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/target/stores - 1 credit (standard)

Find Target stores near a location

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** PlaceList · **Pagination** single page - The upstream returns one fixed set of nearby stores per location (20 for a ZIP, verified live 2026-07-27) and exposes no page or radius parameter.

Returns Target stores near a ZIP code, city, or state, each with its store id, name, full address, phone number, distance from the searched location, open/closed status, timezone, and two weeks of daily opening hours. Store data changes rarely, so it is 1 credit and heavily cached. Note that Target's product pricing is national on this API, so a store id here is for store-locator use rather than for localising prices.

**Query params**

- `query` (required) - Location to search near: a ZIP code (10001), a city (Austin), or a state. Both ZIP and city forms are verified. · e.g. `10001`

```bash
curl "https://www.socialcrawl.dev/v1/target/stores?query=10001" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

<!-- END:GENERATED:ENDPOINTS -->
