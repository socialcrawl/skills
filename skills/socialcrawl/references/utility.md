# Utility

4 endpoints, all GET. Base URL `https://www.socialcrawl.dev`, auth header `x-api-key: $SOCIALCRAWL_API_KEY`.

**Credit costs:** 4 free (0 credits) - the exact cost is in each endpoint heading below.

Free (0-credit) self-discovery endpoints. Point an agent at these to enumerate the live surface at runtime instead of trusting a cached copy of this skill: `/v1/utility/endpoints` lists everything, `/v1/utility/endpoint` explains one endpoint, `/v1/utility/llms` returns the whole agent context corpus, and `/v1/utility/quickstart` returns a ready-to-run first call. They never bill, so they are always safe to call first when unsure.

<!-- BEGIN:GENERATED:ENDPOINTS (auto-generated by generate-docs.ts - AIP-16b) -->

## GET /v1/utility/endpoint - free (0 credits)

How to use any endpoint

**Cost** free (0 credits) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Complete usage guide for a single endpoint: every parameter with type, description and example, the exact credit cost, cache behaviour, pagination recipe, a copy-paste example request, an example response, the response schema URL, and related endpoints. Identify the endpoint by id (tiktok/profile) or url (/v1/tiktok/profile). Free to call.

**Query params**

- `id` (optional, string) - Endpoint id as platform/resource, e.g. tiktok/profile · e.g. `tiktok/profile`
- `url` (optional, string) - Endpoint path form, e.g. /v1/tiktok/profile (a full https URL also works)
- `method` (optional, enum: GET | POST | PATCH | DELETE) - Disambiguates a resource registered under more than one HTTP method, e.g. /v1/web/monitors/{monitor_id} is GET, PATCH and DELETE. Omit it and the first registered method wins

**Constraints**

- Provide at least one of `id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/utility/endpoint?id=tiktok/profile" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/utility/endpoints - free (0 credits)

List every available endpoint

**Cost** free (0 credits) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Machine-readable catalog of every active SocialCrawl endpoint: path, method, platform, credit cost, required and optional parameters, response archetype, and a link to the per-endpoint usage guide. Generated from the endpoint registry at request time, so it never drifts from what is callable. Free to call.

**Query params**

- `platform` (optional, string) - Filter the catalog to a single platform slug (e.g. tiktok) · e.g. `tiktok`
- `search` (optional, string) - Case-insensitive substring filter over endpoint paths and summaries
- `method` (optional, enum: GET | POST | PATCH | DELETE) - Filter by HTTP method (GET, POST, PATCH or DELETE)

```bash
curl "https://www.socialcrawl.dev/v1/utility/endpoints" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/utility/llms - free (0 credits)

AI-agent context payload

**Cost** free (0 credits) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

The SocialCrawl context corpus for AI agents, served through the API: the same content as llms.txt for the whole API or a single platform, as markdown or structured JSON. An agent with an API key can bootstrap itself with one call instead of scraping documentation pages. Free to call.

**Query params**

- `platform` (optional, string) - Scope the context to one platform slug · e.g. `tiktok`
- `format` (optional, enum: markdown | json) - markdown (default) returns the llms corpus text; json returns a structured context object

```bash
curl "https://www.socialcrawl.dev/v1/utility/llms" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/utility/quickstart - free (0 credits)

Get started in one call

**Cost** free (0 credits) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** none

Everything needed for a first successful API call in one response: authentication, base URL, the response envelope, the credit billing model, the error taxonomy, rate limits, pagination rules, and a copy-paste first request. Pass platform to tailor the first-call example. Free to call.

**Query params**

- `platform` (optional, string) - Tailor the first-call example and links to one platform slug · e.g. `tiktok`

```bash
curl "https://www.socialcrawl.dev/v1/utility/quickstart" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

<!-- END:GENERATED:ENDPOINTS -->
