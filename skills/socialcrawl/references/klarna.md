# Klarna

18 endpoints, all GET. Base URL `https://www.socialcrawl.dev`, auth header `x-api-key: $SOCIALCRAWL_API_KEY`.

**Credit costs:** 8 standard (1 credit), 10 advanced (5) - the exact cost is in each endpoint heading below.

Klarna's shopping comparison graph - the widest merchant-offer surface here. One product id resolves to every merchant selling it, its price history, both user and professional reviews, and a side-by-side comparison against another product. Category endpoints go beyond a taxonomy: filters, popular search keywords, and buying-guide copy. Product/Review/Seller rows use the canonical schemas, so they line up with Amazon, Walmart, Target, eBay, and Home Depot.

<!-- BEGIN:GENERATED:ENDPOINTS (auto-generated by generate-docs.ts - AIP-16b) -->

## GET /v1/klarna/categories - 1 credit (standard)

List Klarna root categories

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - The upstream returns the complete root taxonomy in one call (the USA tree was 398KB on 2026-09-02) and exposes no page parameter.

Returns Klarna's browse taxonomy for a region: each node's id, display name, and children. Feed a node id into GET /v1/klarna/category to walk products, or GET /v1/klarna/categories/children for the next level.

**Query params**

- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/categories" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/categories/children - 1 credit (standard)

Get Klarna sub-categories

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - One category node plus its child list in a single call; no page parameter.

Returns one Klarna category and its children. t1 (TV and Audio) returned Headphones as cl94. Unknown category ids return 404 and are not billed.

**Query params**

- `category_id` (required) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `t1`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/categories/children?category_id=t1" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/category - 5 credits (advanced)

Browse Klarna products in a category

**Cost** 5 credits (advanced) · **Cache** 120s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** offset - `offset`

Returns products in a Klarna category, 48 per page by default, each with product id, title, lowest price, rating, and image. Pagination is clean: page 2 of Headphones had 24 new ids and zero overlap with page 1. Feed a category id from GET /v1/klarna/categories.

**Query params**

- `category_id` (required) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `cl94`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `size` (optional, integer, 1-100) - Products per page, 1 to 100. Default 48.
- `offset` (optional, integer, min 0) - Row offset, starting at 0. Prefer the universal cursor parameter.
- `sort_by` (optional, enum: popularity | price_low | price_high | trend | name | rating | price_drop) - Result ordering: popularity (default), price_low, price_high, trend, name, rating, or price_drop.
- `price_range` (optional, string) - Price filter as min-max, for example 50-500, or 50- for 50 and above.
- `filter` (optional, string) - Facet filter as keyID-valueID from GET /v1/klarna/category/filters.
- `category_path_id` (optional, string) - Sub-category path id as keyID-valueID.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/category?category_id=cl94" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/category/filters - 1 credit (standard)

Get Klarna filters for a category

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - Facet groups for one category in a single call; no page parameter.

Returns the facet groups you can pass as filter on GET /v1/klarna/category (brand, price, features). Headphones had 14 groups on 2026-09-02.

**Query params**

- `category_id` (required) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `cl94`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `category_path_id` (optional, string) - Optional sub-category path id as keyID-valueID.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/category/filters?category_id=cl94" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/category/guide - 1 credit (standard)

Get Klarna buying-guide content for a category

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - FAQ, shopping tips, and boards for one category in a single call.

Returns Klarna's buying-guide payload for a category: FAQ, shopping tips, and editorial boards.

**Query params**

- `category_id` (required) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `cl94`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `size` (optional, integer, 1-20) - Number of guiding items, 1 to 20. Default 4.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/category/guide?category_id=cl94" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/category/keywords - 1 credit (standard)

Get popular Klarna search keywords for a category

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - Fixed popular-keyword list per category; no page parameter.

Returns popular search keywords for a Klarna category, for example Marantz under t1. Feed a name into GET /v1/klarna/search.

**Query params**

- `category_id` (required) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `t1`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/category/keywords?category_id=t1" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/product - 5 credits (advanced)

Get a Klarna product by id or URL

**Cost** 5 credits (advanced) · **Cache** 600s (a hit costs 0 credits) · **Returns** Product · **Pagination** none

Returns one Klarna shopping product: title, description, brand, rating, review count, and specifications. This payload has no list price; call GET /v1/klarna/product/offers for merchant prices. Pass the numeric product id plus category_id (cl94), or pass the full shopping URL and both ids are extracted. Ghost ids return 404 and are not billed.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `category_id` (optional, string) - Klarna category id, for example cl94 for Headphones or t1 for TV and Audio. Ids come from GET /v1/klarna/categories and from search rows as category.id. Required for GET /v1/klarna/product when you pass product_id instead of a shopping URL. · e.g. `cl94`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/product?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/product/compare - 5 credits (advanced)

Compare two Klarna products

**Cost** 5 credits (advanced) · **Cache** 600s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** single page - Compare returns exactly two product cards. No page parameter.

Returns two Klarna products side by side: title, brand, rating, lowest price, and image. Pass two product ids from search or category.

**Query params**

- `product_id` (required) - First Klarna product id. · e.g. `3331931731`
- `product_id_2` (required) - Second Klarna product id. · e.g. `3216399795`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/product/compare?product_id=3331931731&product_id_2=3216399795" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/product/offers - 5 credits (advanced)

List merchant offers for a Klarna product

**Cost** 5 credits (advanced) · **Cache** 600s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** single page - product-page-details returns the full offer set for one product in a single call (40 offers on AirPods 4, 2026-09-02) and exposes no page parameter.

Returns the merchant offers Klarna compared for one product, each with seller name, price, currency, stock status, and shipping cost. AirPods 4 had 40 offers across 19 merchants on 2026-09-02, cheapest Walmart at 110 USD. Ghost ids return 404 and are not billed.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/product/offers?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/product/price-history - 5 credits (advanced)

Get Klarna price history for a product

**Cost** 5 credits (advanced) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - The upstream returns the full history for the chosen interval in one call (93 points over three months, 723 over INFINITE_DAYS on AirPods 4) and exposes no page parameter.

Returns historical merchant prices for a product: lowest, highest, currency, per-merchant time series, and the merchant directory. interval is ONE_MONTH, THREE_MONTHS (default), or INFINITE_DAYS.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `interval` (optional, enum: ONE_MONTH | THREE_MONTHS | INFINITE_DAYS) - History window: ONE_MONTH, THREE_MONTHS (default), or INFINITE_DAYS.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/product/price-history?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/reviews - 5 credits (advanced)

Get Klarna user reviews for a product

**Cost** 5 credits (advanced) · **Cache** 300s (a hit costs 0 credits) · **Returns** ReviewList · **Pagination** cursor - `nextBatchId`, page size `limit`

Returns written user reviews aggregated across merchants, 10 per page by default, each with text, star rating, title, reviewer name, source domain, and date. Pagination is clean: page 2 of AirPods 4 had 0 overlapping extracts with page 1. Pass the returned cursor back until has_more is false.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `limit` (optional, integer, 1-50) - Reviews per page, 1 to 50. Default 10.
- `nextBatchId` (optional, integer, min 0) - Pagination token from the previous page. Prefer the universal cursor parameter.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/reviews?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/reviews/overview - 5 credits (advanced)

Get Klarna review totals and score distributions

**Cost** 5 credits (advanced) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - One mixed payload of score, distributions, and a first page of user plus pro reviews. Dedicated list endpoints paginate the review bodies.

Returns the review summary for a product: average score, user and professional counts, star distributions, and a first page of each review type. For the full review text use GET /v1/klarna/reviews or GET /v1/klarna/reviews/pro.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/reviews/overview?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/reviews/pro - 5 credits (advanced)

Get Klarna professional reviews for a product

**Cost** 5 credits (advanced) · **Cache** 300s (a hit costs 0 credits) · **Returns** ReviewList · **Pagination** cursor - `nextBatchId`, page size `limit`

Returns professional and expert reviews for a Klarna product, 10 per page, each with text, score, source, and date. AirPods 4 had 21 professional reviews; page 2 returned 8 rows and terminated.

**Query params**

- `product_id` (optional, string) - Klarna product id, the numeric id in a shopping URL: klarna.com/us/shopping/pl/cl94/3331931731/... is product id 3331931731. Product ids also come back on every row of GET /v1/klarna/search and GET /v1/klarna/category. · e.g. `3331931731`
- `url` (optional, string) - Full Klarna shopping URL, for example https://www.klarna.com/us/shopping/pl/cl94/3331931731/Headphones/Apple-AirPods-4-with-Active-Noise-Cancellation-White/. The product id and category id are extracted from the path.
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `limit` (optional, integer, 1-50) - Reviews per page, 1 to 50. Default 10.
- `nextBatchId` (optional, integer, min 0) - Pagination token from the previous page. Prefer the universal cursor parameter.

**Constraints**

- Provide at least one of `product_id`, `url`.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/reviews/pro?product_id=3331931731" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/search - 5 credits (advanced)

Search Klarna products by keyword

**Cost** 5 credits (advanced) · **Cache** 120s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** single page - Upstream /product-search returns a fixed 20-row page. offset, size, page, and sortType were ignored live on 2026-09-02 (byte-identical 24040B bodies).

Returns Klarna shopping products matching a keyword, 20 per call, each with product id, title, category, lowest price, rating, and image. Pagination params are not honoured by upstream. A query that matches nothing is returned as an empty page and is not billed.

**Query params**

- `query` (required) - Search keyword, for example headphone or airpods. · e.g. `headphone`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/search?query=headphone" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/search/suggestions - 1 credit (standard)

Get Klarna search suggestions

**Cost** 1 credit (standard) · **Cache** 120s (a hit costs 0 credits) · **Returns** SearchResult · **Pagination** single page - Autocomplete: one products list plus one suggestions list, no page parameter.

Returns Klarna search autocomplete: matching products plus category and feature suggestions for a partial query.

**Query params**

- `query` (required) - Partial search query to autocomplete, for example headphone. · e.g. `headphone`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/search/suggestions?query=headphone" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/store/filters - 1 credit (standard)

Get Klarna filters for a store page

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** Analytics · **Pagination** single page - Facet dump for one store in a single call (Walmart was 219KB / 1000 brand counts).

Returns the facet groups for one Klarna store (brands and other filters). Feed values into GET /v1/klarna/store/products.

**Query params**

- `id` (required) - Klarna store UUID, the last segment of storeKrn: 09f4d635-0452-4f63-9455-c45420d95669 for Walmart US. Ids come back on every row of GET /v1/klarna/stores. · e.g. `09f4d635-0452-4f63-9455-c45420d95669`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/store/filters?id=09f4d635-0452-4f63-9455-c45420d95669" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/store/products - 5 credits (advanced)

List products from a Klarna store

**Cost** 5 credits (advanced) · **Cache** 120s (a hit costs 0 credits) · **Returns** ProductList · **Pagination** offset - `offset`

Returns products sold by one Klarna retailer, 25 per page by default. Pagination is clean: Walmart page 2 had 25 new ids and zero overlap with page 1. Unknown store ids return an empty page and are not billed.

**Query params**

- `id` (required) - Klarna store UUID, the last segment of storeKrn: 09f4d635-0452-4f63-9455-c45420d95669 for Walmart US. Ids come back on every row of GET /v1/klarna/stores. · e.g. `09f4d635-0452-4f63-9455-c45420d95669`
- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `size` (optional, integer, 1-100) - Products per page, 1 to 100. Default 25.
- `offset` (optional, integer, min 0) - Row offset, starting at 0. Prefer the universal cursor parameter.
- `sort_by` (optional, enum: popularity | price_low | price_high | trend | name | rating | price_drop) - Result ordering: popularity (default), price_low, price_high, trend, name, rating, or price_drop.
- `price_range` (optional, string) - Price filter as min-max, for example 50-500, or 50- for 50 and above.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/store/products?id=09f4d635-0452-4f63-9455-c45420d95669" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

## GET /v1/klarna/stores - 1 credit (standard)

List Klarna shopping stores

**Cost** 1 credit (standard) · **Cache** 1800s (a hit costs 0 credits) · **Returns** SellerList · **Pagination** offset - `offset`

Returns Klarna's retailer directory, 24 per page, each with store UUID, display name, Klarna store URL, icons, and checkout flags. Pagination is clean: page 2 had 24 new ids and zero overlap with page 1. USA totalHits was 10000 on 2026-09-02. Optional filters: categories (fashion, travel), cashback, shop_in_app, klarna_at_checkout.

**Query params**

- `region` (optional, enum: usa | uk | germany | sweden | france | italy | spain | netherlands | austria | ireland | norway | finland | denmark) - Shopping region: usa (default), uk, germany, sweden, france, italy, spain, netherlands, austria, ireland, norway, finland, denmark. Changes currency, language, and the merchant set.
- `size` (optional, integer, 1-100) - Stores per page, 1 to 100. Default 24.
- `offset` (optional, integer, min 0) - Row offset, starting at 0. Prefer the universal cursor parameter.
- `categories` (optional, string) - Store category filter, for example fashion or travel.
- `cashback` (optional, boolean) - When true, keep stores that offer cashback.
- `shop_in_app` (optional, boolean) - When true, keep stores available in the Klarna app.
- `klarna_at_checkout` (optional, boolean) - When true, keep stores with Klarna at checkout.

```bash
curl "https://www.socialcrawl.dev/v1/klarna/stores" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

<!-- END:GENERATED:ENDPOINTS -->
